{
    "safety_check": checks if you are sure before removing files

    
    "pre_clean": cleans files
    
    "setup": make sure python 3.9 is installed, create shortcut if "y"
    
    "create": makes directory for game
    
    "download": downloads files from github respository
    
    "post_clean": cleans up files afterwards
    
    "quit_install": quits after a countdown/delay
}